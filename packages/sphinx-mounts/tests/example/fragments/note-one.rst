Fragment one
============

FRAGMENT_ONE_MARKER

A loose file with no bundle index. It is mounted in file-list mode with
``attach_each = true``, so it is wired directly into the host toctree — no
``index.rst`` is needed to stitch the fragments together.
