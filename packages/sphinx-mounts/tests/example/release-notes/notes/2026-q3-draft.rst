2026 Q3 (draft)
===============

RELEASE_NOTES_DRAFT_MARKER

This file sits in the same directory as the mounted notes but is left out
of the mount's ``files`` list. In directory mode it would be discovered and
mounted automatically; in file-list mode nothing is walked, so this draft
never becomes a docname and never appears in the built site.
