Bazel-mounted host project
==========================

The host project's only native page. The toctree below references content
that lives entirely in ``bazel-bin/docs/`` and is read in place by
sphinx-mounts.

.. toctree::
   :maxdepth: 2

   _bazel/index
