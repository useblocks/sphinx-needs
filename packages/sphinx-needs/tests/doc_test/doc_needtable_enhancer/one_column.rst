A ONE-COLUMN TABLE
==================

One column, so the "a table cannot be reduced to no columns" guard has to hold from the
first paint: with two or more columns the reader's first click engages it in time, and with
exactly one that click is already the fatal one.

.. needtable::
   :columns: id
   :filter: docname == "index"
