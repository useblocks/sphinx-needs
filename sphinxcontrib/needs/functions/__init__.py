from sphinxcontrib.needs.functions.functions import register_func, execute_func, FunctionParsingException

from sphinxcontrib.needs.functions.common import test, copy

needs_common_functions = [test, copy]
